# Build output goes to src-tauri/target by default so CI and local builds agree.
# To reuse a shared cache locally, set CARGO_TARGET_DIR instead of hardcoding a
# path here (build_tauri_exe.bat already does this).
[build]
